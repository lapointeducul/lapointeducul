service firebase.storage {
  match /b/{bucket}/o {
    match /LPDC {
      match /{episode} {
        allow read, write
      }
    }
    match /rss {
      allow read, write
    }
    match /{allPaths=**} {
      allow read, write: if request.auth!=null;
    }
  }
}
